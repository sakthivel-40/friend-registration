<?php
ini_set('display_errors', 1);
error_reporting(E_ALL);

$host   = "friend-db.ct824eigcxf1.ap-south-1.rds.amazonaws.com";
$user   = "admin";
$pass   = "Admin1234";
$db     = "friendsdb";

$conn = new mysqli($host, $user, $pass, $db);
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

$sql = "SELECT name, email, image_url FROM friends";
$result = $conn->query($sql);

echo "<h2>Registered Friends</h2>";
echo "<table border='1' cellpadding='10'>";
echo "<tr><th>Name</th><th>Email</th><th>Image</th></tr>";

if ($result->num_rows > 0) {
    while ($row = $result->fetch_assoc()) {
        echo "<tr>";
        echo "<td>" . htmlspecialchars($row['name']) . "</td>";
        echo "<td>" . htmlspecialchars($row['email']) . "</td>";
        echo "<td><img src='" . htmlspecialchars($row['image_url']) . "' width='100'></td>";
        echo "</tr>";
    }
} else {
    echo "<tr><td colspan='3'>No friends registered yet.</td></tr>";
}

echo "</table>";
$conn->close();
?>
